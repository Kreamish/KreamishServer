spring:
  datasource:
    url: jdbc:mysql://localhost:1521/kreamishdb?serverTimezone=UTC
    username: kreamishwas
    password: kreamish
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database: h2
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #show_sql: true
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
